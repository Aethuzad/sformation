{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
<section class='w-50 mx-auto'>
    <h1 class='text-center mt-5'>Register</h1>

    {{ form_errors(registrationForm) }}

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.nom) }}
        {{ form_row(registrationForm.prenom) }}
        {{ form_row(registrationForm.age) }}
        {{ form_row(registrationForm.telephone) }}
        {{ form_row(registrationForm.adresse) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
        {{ form_row(registrationForm.agreeTerms) }}

        <button type="submit" class="btn btn-primary">Register</button>
    {{ form_end(registrationForm) }}
    </section>
{% endblock %}

{# {% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
<section class="vh-100" style="background-color: #eee;">
  <div class="container h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-lg-12 col-xl-11">
        <div class="card text-black">
          <div class="card-body p-md-5">
            <div class="row justify-content-center">
              <div class="col-md-10 col-lg-6 col-xl-5 order-2 order-lg-1">

                <p class="text-center h1 fw-bold mb-5 mx-1 mx-md-4 mt-4">Sign up</p>
                <form class="mx-1 mx-md-4" name="registration_form" method="post">
                {{ form_errors(registrationForm) }}


                {# {{ form_row(registrationForm.nom)}} #}
                {# Input name #}
                  {# <div class="d-flex flex-row align-items-center mb-4">
                    <i class="fas fa-envelope fa-lg me-3 fa-fw"></i>
                    <div class="form-outline flex-fill mb-0">
                      <input type="text" id="registration_form_nom" name="registration_form[nom]" required="required" class="form-control" maxlength="180" />
                      <label class="required" for="registration_form_nom">Your name</label>
                    </div>
                  </div>
                {# {{ form_row(registrationForm.prenom)}} #}
                    {# <div class="d-flex flex-row align-items-center mb-4">
                    <i class="fas fa-envelope fa-lg me-3 fa-fw"></i>
                    <div class="form-outline flex-fill mb-0">
                      <input type="text" id="registration_form_prenom" name="registration_form[prenom]" required="required" class="form-control" maxlength="180" />
                      <label class="required" for="registration_form_prenom">Your lastname</label>
                    </div>
                  </div> #}
                {# {{ form_row(registrationForm.age)}} #}
                    {# <div class="d-flex flex-row align-items-center mb-4">
                    <i class="fas fa-envelope fa-lg me-3 fa-fw"></i>
                    <div class="form-outline flex-fill mb-0">
                      <input type="text" id="registration_form_age" name="registration_form[age]" required="required" class="form-control" maxlength="180" />
                      <label class="required" for="registration_form_age">Your age</label>
                    </div>
                  </div> #}
                {# {{ form_row(registrationForm.telephone)}} #}
                    {# <div class="d-flex flex-row align-items-center mb-4">
                    <i class="fas fa-envelope fa-lg me-3 fa-fw"></i>
                    <div class="form-outline flex-fill mb-0">
                      <input type="text" id="registration_form_telephone" name="registration_form[telephone]" required="required" class="form-control" maxlength="180" />
                      <label class="required" for="registration_form_telephone">Your number</label>
                    </div>
                  </div> #}
                {# {{ form_row(registrationForm.adresse)}} #}
                    {# <div class="d-flex flex-row align-items-center mb-4">
                    <i class="fas fa-envelope fa-lg me-3 fa-fw"></i>
                    <div class="form-outline flex-fill mb-0">
                      <input type="text" id="registration_form_adresse" name="registration_form[adresse]" required="required" class="form-control" maxlength="180" />
                      <label class="required" for="registration_form_adresse">Your adress</label>
                    </div>
                  </div> #}

                {# Input email #}
                  {# <div class="d-flex flex-row align-items-center mb-4">
                    <i class="fas fa-envelope fa-lg me-3 fa-fw"></i>
                    <div class="form-outline flex-fill mb-0">
                      <input type="text" id="registration_form_email" name="registration_form[email]" required="required" class="form-control" maxlength="180" />
                      <label class="required" for="registration_form_email">Your Email</label>
                    </div>
                  </div> #}

                {# Input password #}
                  {# <div class="d-flex flex-row align-items-center mb-4">
                    <i class="fas fa-lock fa-lg me-3 fa-fw"></i>
                    <div class="form-outline flex-fill mb-0">
                      <input type="password" id="registration_form_plainPassword" name="registration_form[plainPassword]" class="form-control" required="required" autocomplete="new-password"/>
                      <label  for="registration_form_plainPassword" class="required form-label">Password</label>
                    </div>
                  </div> #}

                {# Input checkbox #}
                  {# <div class="form-check d-flex justify-content-center mb-5">
                    <input class="form-check-input me-2" type="checkbox"  id="registration_form_agreeTerms" name="registration_form[agreeTerms]" required="required" value="1" />
                    <label class="form-check-label required" for="registration_form_agreeTerms">
                      I agree all statements in <a href="#!">Terms of service</a>
                    </label>
                  </div> #}

                {# Input button #}
                  {# <div class="d-flex justify-content-center mx-4 mb-3 mb-lg-4">
                    <button type="submit" class="btn btn-primary btn-lg">Register</button>
                  </div>
                
                <input type="hidden" id="registration_form__token" name="registration_form[_token]" value="1dfd28b4518846481934eaf1ab7f288.XkopKhFu5tDee3E0K3ixEbfI59SwXPOCf9T2dAaiAUc.ESRBbFlXtry1CUdmWkzCetKMoabSJKfNEaayLErGVwYqM25mQimkgoQTAg">

                </form>
                    
              </div>
              <div class="col-md-10 col-lg-6 col-xl-7 d-flex align-items-center order-1 order-lg-2">

                <img src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-registration/draw1.webp"
                  class="img-fluid" alt="Sample image">

              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section> #}
{# {% endblock %} #}